# Phase 6 DAG Validation

After the dependency graph is validated, analyze:

- critical path
- parallelizable task batches
- bottleneck tasks with many dependents

Use these findings to refine the revision sequence rather than treating the initial manual order as final.
