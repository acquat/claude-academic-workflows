# Revision Task Schema

Each task should include:

- `task_id`
- `source`
- `summary`
- `status`
- `dependencies`
- `section_or_artifact`
- `evidence`

The graph must be acyclic before it is used to sequence work.
