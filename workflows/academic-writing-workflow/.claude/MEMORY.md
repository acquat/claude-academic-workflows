# MEMORY — Academic Writing Workflow

Append-only `[LEARN]` log. `[LEARN:category] wrong → right (why)`. Loads every session — keep it short.

## Seed lessons (transferable; keep or replace)

- `[LEARN:rigor]` Draft numbers/predictors/line-cites from the *source in the current turn*, never from session memory → the recorded-incidents log in `no-fabrication.md` exists because this is the #1 error.
- `[LEARN:workflow]` Every paper read → log it in `lit_review.md` before citing → re-reading PDFs is expensive and uncited memory is unverifiable.

## Project-specific lessons

<!-- add as you go: notation pins, journal quirks, referee preferences, replication gotchas -->
