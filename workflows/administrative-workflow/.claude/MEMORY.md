# MEMORY — Administrative Workflow

Append-only `[LEARN]` log. When the user corrects something or a non-obvious lesson emerges, add a line:
`[LEARN:category] wrong → right (why)`. Keep it short; this loads every session.

## Seed lessons (transferable; keep or replace)

- `[LEARN:forms]` Spreadsheet totals written by openpyxl as `=SUM()` cache to `None` and print blank → always **hardcode subtotals/totals as numeric values** and verify by re-opening.
- `[LEARN:deadlines]` File against the *expense date*, not today → the deadline clock starts at purchase.

## Project-specific lessons

<!-- add as you go, e.g.:
- [LEARN:policy] Software subscriptions go under "Other costs", not "Travel".
- [LEARN:routing] Vendor X must go through procurement, not personal reimbursement.
-->
