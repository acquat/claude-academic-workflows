# Stata Syntax Reference (internal, growing)

> Curated, verified Stata syntax notes accumulated across sessions. `/stata-syntax` reads this **first**.
> Append a new entry whenever you had to consult a manual (format below). Keep it short enough to fit one Read.

## Entry format

```
### <command [, key option]>
- Manual: <file.pdf, section>
- Verified syntax:
  ```stata
  <one minimal example>
  ```
- Gotchas: <anything that surprised you>
- Date: <YYYY-MM-DD>
```

---

## Data management

<!-- e.g. merge, reshape, collapse, egen -->

## Estimation

<!-- e.g. regress, logit, ivregress, margins -->

## Postestimation

<!-- e.g. predict variants, test, lincom -->

## Graphics

<!-- e.g. twoway, graph export -->

## User-written commands (help not in docs)

<!-- e.g. reghdfe, ivreghdfe, coefplot, esttab, outreg2 — record the verified syntax the user confirmed -->
