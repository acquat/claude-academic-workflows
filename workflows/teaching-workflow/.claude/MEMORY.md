# MEMORY — Teaching Workflow

Append-only `[LEARN]` log. `[LEARN:category] wrong → right (why)`. Loads every session — keep it short.

## Seed lessons (transferable; keep or replace)

- `[LEARN:teaching]` Feedback is a separate memo; never edit the student's file → integrity of the record and student trust.
- `[LEARN:tutorials]` Every TF exercise must use a method actually taught in the target lecture; the handout never contains solutions → match notation, keep answers in the separate solutions file.

## Per-course conventions

<!-- record per-course conventions (notation, language, submission formats) here as they stabilize -->
